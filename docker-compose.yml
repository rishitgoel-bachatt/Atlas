# services:
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: hermes_postgres
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER:-hermes_user}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-hermes_pass}
  #     POSTGRES_DB: ${POSTGRES_DB:-hermes}
  #   ports:
  #     - '15433:5432'
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: always
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U hermes_user -d hermes']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # keycloak:
  #   image: quay.io/keycloak/keycloak:24.0
  #   container_name: hermes_keycloak
  #   command: start-dev --http-port 8080 --health-enabled=true
  #   environment:
  #     KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
  #     KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin_password}
  #   ports:
  #     - '8080:8080'
  #   # depends_on:
  #   #   postgres:
  #   #     condition: service_healthy
  #   restart: always
  #   healthcheck:
  #     test: ['CMD', 'bash', '-c', 'exec 3<>/dev/tcp/localhost/8080 && echo -e "GET /health/ready HTTP/1.1\r\nHost: localhost\r\nConnection: close\r\n\r\n" >&3 && cat <&3 | grep -q "UP"']
  #     interval: 15s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 30s

  # ── Redash Dependencies ──
  # redis:
  #   image: redis:7-alpine
  #   container_name: hermes_redis
  #   ports:
  #     - '16379:6379'
  #   restart: always
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # redash-postgres:
  #   image: postgres:15-alpine
  #   container_name: hermes_redash_postgres
  #   environment:
  #     POSTGRES_USER: redash
  #     POSTGRES_PASSWORD: redash_pass
  #     POSTGRES_DB: redash
  #   ports:
  #     - '15434:5432'
  #   volumes:
  #     - redash_postgres_data:/var/lib/postgresql/data
  #   restart: always
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U redash -d redash']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # ── Redash Server ──
  # redash:
  #   image: redash/redash:latest
  #   container_name: hermes_redash
  #   command: server
  #   environment:
  #     REDASH_DATABASE_URL: 'postgresql://redash:redash_pass@redash-postgres:5432/redash'
  #     REDASH_REDIS_URL: 'redis://redis:6379/0'
  #     REDASH_WEB_WORKERS: '4'
  #     REDASH_COOKIE_SECRET: ${REDASH_COOKIE_SECRET:-super_secret_cookie}
  #     PYTHONUNBUFFERED: '0'
  #   ports:
  #     - '5500:5000'
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     redash-postgres:
  #       condition: service_healthy
  #   restart: always
  #   healthcheck:
  #     test: ['CMD', 'curl', '-f', 'http://localhost:5000/ping']
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #     start_period: 60s

  # ── Redash Workers (background jobs) ──
  # redash-worker:
  #   image: redash/redash:latest
  #   container_name: hermes_redash_worker
  #   command: worker
  #   environment:
  #     REDASH_DATABASE_URL: 'postgresql://redash:redash_pass@redash-postgres:5432/redash'
  #     REDASH_REDIS_URL: 'redis://redis:6379/0'
  #     REDASH_WEB_WORKERS: '1'
  #     REDASH_COOKIE_SECRET: ${REDASH_COOKIE_SECRET:-super_secret_cookie}
  #     PYTHONUNBUFFERED: '0'
  #     QUEUES: 'queries,scheduled_queries,celery'
  #     WORKERS_COUNT: '2'
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     redash-postgres:
  #       condition: service_healthy
  #   restart: always

  # ── Redash Scheduler ──
  # redash-scheduler:
  #   image: redash/redash:latest
  #   container_name: hermes_redash_scheduler
  #   command: scheduler
  #   environment:
  #     REDASH_DATABASE_URL: 'postgresql://redash:redash_pass@redash-postgres:5432/redash'
  #     REDASH_REDIS_URL: 'redis://redis:6379/0'
  #     REDASH_COOKIE_SECRET: ${REDASH_COOKIE_SECRET:-super_secret_cookie}
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     redash-postgres:
  #       condition: service_healthy
  #   restart: always

# volumes:
#   postgres_data:
#   redash_postgres_data:
